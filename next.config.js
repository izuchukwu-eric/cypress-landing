/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        serverActions: true
    },
    images: {
        domains: ['qsaooomjiqirgyafqqjz.supabase.co']
    }
}

module.exports = nextConfig
